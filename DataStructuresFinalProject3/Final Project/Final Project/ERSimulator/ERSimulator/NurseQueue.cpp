#include "stdafx.h"
#include "NurseQueue.h"

void NurseQueue::update()
{
	if (r_queue.empty())
	{
		int startTime = 0;
		int endTime = rand() % 10;
		// take resident from top of waiting room
	}
}
		//else {
		//			if (startTime == endTime)
		//			update resident treatment time
		//			push resident back to Residents vector
		//		}
		// startTime++
